#!/usr/bin/env python
"""
Utility methods for dealing with tracks
"""

import midi


def contains_harmony(track):
    # last_event = None
    # for event in track:
    #     if last_event is None:
    #         last_event = event
    #         continue
    #
    #     if isinstance(last_event, midi.NoteOnEvent) and isinstance(event, midi.NoteOnEvent):
    #         return True
    #
    #     last_event = event

    return False
